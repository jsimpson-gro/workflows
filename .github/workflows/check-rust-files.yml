on:
  push:
    paths:
      - '**/*.rs'

jobs:
  rust_merge_check:
    runs-on: ubuntu-latest
    steps:
      - env:
          ISSUE_URL: ${{ github.event.issue.html_url }}
        run: |
          echo "Checking rust files."
          true
